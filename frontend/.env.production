# Default retained for local production builds of the temporary demo. Hosted
# deployments override this public build-time value without editing this file;
# the GitHub Pages workflow uses its repository variable when one is present.
VITE_API_BASE_URL=https://ortodynamic.onrender.com/api/v1
