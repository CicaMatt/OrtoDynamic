# Loaded automatically by `vite build` (production mode) and inlined into the
# bundle. This is a public URL, not a secret. It points the deployed frontend at
# the Render API. Change it here (and rebuild) if the backend URL changes.
VITE_API_BASE_URL=https://ortodynamic.onrender.com/api/v1
