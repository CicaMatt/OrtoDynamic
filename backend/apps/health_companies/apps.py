from django.apps import AppConfig


class HealthCompaniesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.health_companies"
    verbose_name = "Aziende Sanitarie"
